# Jib configuration for handling large image pushes to GHCR
# Increase timeouts to prevent "Read timed out" errors during blob uploads
systemProp.jib.httpTimeout=120000
systemProp.jib.serialize=true

# Gradle daemon configuration
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
